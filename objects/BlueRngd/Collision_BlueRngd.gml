/*  
    var _dir = point_direction(other.x, other.y, x, y);
    var _dist = point_distance(x, y, other.x, other.y);
    var _push_strength = 5


    if (_dist < sprite_width / 2 + other.sprite_width / 2) && id != other.id
    {
        x += lengthdir_x(_push_strength, _dir);
        y += lengthdir_y(_push_strength, _dir);
    }
	
	
	
	
	
	
	